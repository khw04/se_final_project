VITE_POKEMO_MOCK_SESSION=true

# 소셜 로그인 client id (공개 값). 발급 후 채우면 로그인 화면의 소셜 버튼이 동작함
VITE_GOOGLE_CLIENT_ID=
VITE_KAKAO_CLIENT_ID=
# 선택: 기본값은 현재 origin. 커스텀 redirect URI가 필요할 때만 설정
# VITE_OAUTH_REDIRECT_URI=http://localhost:5173
