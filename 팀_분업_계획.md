# Pokemo 4인 분업 + 바이브 코딩 계획

팀원 4명(A / B / C / D)이 각자 AI 바이브 코딩으로 병렬 개발하기 위한 분업·충돌방지·프롬프트 문서.
기준 문서: `doc/Pokemo_요구사항_문서_v2.3.pdf`, `doc/Pokemo_설계문서_v3_5.pdf`, 진행상황: `진행상황.md`.

---

## 1. 한눈에 보는 분업

| 담당 | 백엔드 패키지 | 프론트 폴더 | 핵심 기능 |
|---|---|---|---|
| **A** 인증/관리자/공지 | `com.pokemo.auth`(확장), `com.pokemo.notice` | `screens/notice/`, `screens/auth/` | 이메일 인증, 로그아웃/revoke, 비번찾기, 관리자, 공지 CRUD |
| **B** 일정/노트 | `com.pokemo.calendar`, `com.pokemo.note`, `com.pokemo.subject`(확장) | `screens/calendar/`, `screens/note/` | 과목, 일정 CRUD/반복/D-Day, 노트 MD/태그/검색/자동저장 |
| **C** 퀴즈/통계 | `com.pokemo.quiz`, `com.pokemo.stats` | `screens/quiz/`, `screens/wrong/`, `screens/stats/` | 퀴즈 풀이/채점, 오답노트, 정답률·성취도·추이 통계 |
| **D** AI/통합/배포 | `com.pokemo.ai`, **0단계 기반** | `screens/ai/`, `screens/dashboard/`, **공유 골격** | AI 요약/퀴즈생성/추천, 대시보드, Docker compose, 최종 통합 |

> **풀스택 슬라이스 원칙**: 각자 자기 백엔드 + 그 화면까지 함께 담당한다. 프론트를 한 사람에게 몰지 않는다.

---

## 2. 충돌 방지 핵심 — "0단계"를 D가 먼저 끝낸다

현재 프론트는 아래 **5개 공유 파일**에 모든 화면이 몰려 있어, 그대로 병렬 작업하면 무조건 머지 충돌이 난다.

| 충돌 파일 | 이유 |
|---|---|
| `frontend/src/app/pokemoApi.ts` (616줄) | 모든 도메인 mock API가 한 파일 |
| `frontend/src/app/types.ts` | 모든 타입 + `ViewId` 유니온이 한 파일 |
| `frontend/src/app/AppShell.tsx` | `if (view===...)` 라우팅에 화면 추가 시 전원 수정 |
| `frontend/src/app/components/Sidebar.tsx` | `NAV_SECTIONS` 하드코딩 메뉴 |
| (위 `ViewId`) | 화면 추가마다 같은 줄 수정 |

**해결**: D가 0단계에서 이 파일들을 "도메인별 등록 방식"으로 분해·머지한다. 그 후로는 각자
`screens/{도메인}/` + `app/api/{도메인}Api.ts` + `app/types/{도메인}.ts` + `app/nav/registry.ts`에 **import 1줄**만 추가하면 끝 → 충돌 사라짐.

### 백엔드 공유 지점 (3개뿐)
- `auth.security.SecurityConfig` → **A만 수정**. 새 public/ADMIN 경로 필요 시 A에게 요청. 대부분 엔드포인트는 기본 `authenticated()`라 수정 불필요.
- `Subject` 엔티티(일정/노트/퀴즈/통계 공유) → **0단계에서 D가 `com.pokemo.subject`에 미리 생성**. B가 CRUD 확장.
- 공통 예외/에러응답 → **0단계에서 D가 `com.pokemo.common` 생성**. 각 도메인은 `ApiException`만 던짐.

---

## 3. 공유 파일 수정 권한 (이 표가 충돌방지 규칙)

| 파일/패키지 | 수정 가능자 |
|---|---|
| `auth.security.SecurityConfig` | **A만** |
| `app/api/client.ts`, `AppShell.tsx`, `Sidebar.tsx`, `nav/registry.ts` 스켈레톤 | **D만** (단 `registry.ts`에 "자기 nav import 1줄"은 각자 추가 허용) |
| `com.pokemo.common`, `com.pokemo.subject` 엔티티 | **D가 0단계 생성**, 이후 B가 Subject CRUD만 확장 |
| `app/types/index.ts` (re-export) | D만 |
| `진행상황.md` | **D만** |

---

## 4. git 협업 / 머지 순서

- 각자 **feature 브랜치 + PR → main**. 브랜치명: `feat/auth`, `feat/calendar-note`, `feat/quiz-stats`, `feat/foundation`·`feat/ai`·`feat/deploy`.
- PR마다 빌드/테스트 통과 첨부: 백 `backend\mvnw.cmd test`, 프론트 `npm run lint && npm run test && npm run build`.
- **머지 순서**
  1. **D: 0단계 기반** (common/subject + 프론트 api/types/registry 분해) ← 최우선. 머지 전 다른 프론트 작업 보류
  2. A/B/C 백엔드 도메인 (병렬, 서로 독립) — *백엔드는 0단계 백엔드만 머지되면 바로 시작 가능*
  3. A/B/C 프론트 화면 (각자 폴더 + registry 1줄)
  4. D: AI 백엔드 + 화면 연결 + 대시보드 통합
  5. D: Docker compose + 최종 통합/발표 안정화
- 공유 파일 충돌 시: "수정 가능자"가 먼저 머지 → 나머지 rebase.

---

## 5. 기존 컨벤션 (전원 준수)

**백엔드**: Java 21, Spring Boot 3.4.1. 패키지 `com.pokemo.{도메인}.{api,domain,repository,service}`.
DTO는 **record + jakarta validation**, 컨트롤러/서비스 **생성자 주입**, 엔티티는 `@GeneratedValue(IDENTITY)` + `OffsetDateTime` + **setter 없음**.
예외는 `com.pokemo.common.ApiException(status,message)` 던짐. 테스트는 `@SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("test")` + MockMvc. 빌드 `backend\mvnw.cmd test`.

**프론트**: React 19, **react-router 미사용**(nav 레지스트리 기반). fetch만(axios X), API는 `app/api/{도메인}Api.ts` → `app/api/client.ts`로만. 화면은 `useApi` 훅. 빌드 `npm run lint / test / build`.

**인증**: `localStorage` 키 `pokemo.auth`, `Authorization: Bearer {accessToken}`. base URL `VITE_API_BASE_URL`(기본 `http://localhost:8080`).

---

## 6. AI 모델 (무료, D 담당)

| 후보 | 무료 한도/속도 | 비고 |
|---|---|---|
| **Google Gemini 2.0 Flash** ★추천 | 무료 키(AI Studio), 일 ~1,500요청·분당 ~15요청, 빠름 | REST 단순, JSON 출력 안정. 키 발급: aistudio.google.com |
| **Groq (llama-3.3-70b-versatile)** | 무료, 초고속(수백 tok/s) | 분당 한도 있음. 백업/대체용 |

- `com.pokemo.ai`에 **`AiClient` 인터페이스** → `GeminiAiClient` 구현. 나중에 Groq로 교체 가능.
- 설정 `application.yml`: `pokemo.ai.api-key: ${GEMINI_API_KEY:}`, `pokemo.ai.model: gemini-2.0-flash`. **키는 환경변수만**(클라이언트 노출 금지).
- 안정성 요구: 타임아웃 `503`, JSON 파싱 실패 시 DB 저장 금지, AI 오류 시 정답률만 반환(graceful degradation).

---

## 7. 바로 붙여넣는 바이브 코딩 프롬프트

각자 자기 AI 도구에 **[공통 머리말] + 자기 프롬프트**를 붙여넣는다.

### [공통 머리말] — 모든 프롬프트 앞에 붙이기
```
너는 Pokemo(Spring Boot 3.4.1/Java 21 백엔드 + React 19/Vite/TS 프론트) 졸업작품의 한 파트를 맡았다.
규칙:
- 모든 응답·주석·커밋은 한국어.
- 기존 컨벤션 준수: 백엔드 패키지 com.pokemo.{도메인}.{api,domain,repository,service}, DTO는 record+jakarta validation,
  컨트롤러/서비스 생성자 주입, 엔티티는 @GeneratedValue(IDENTITY)+OffsetDateTime+setter 없음, 예외는 com.pokemo.common.ApiException(status,message)을 던짐.
  테스트는 @SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("test") + MockMvc.
- 프론트는 react-router 안 씀(nav 레지스트리 기반). API 호출은 app/api/{도메인}Api.ts에서 app/api/client.ts(fetch 래퍼)로만. 화면은 useApi 훅 사용.
- 인증: localStorage 키 pokemo.auth, Authorization: Bearer {accessToken}.
- 내 소유 경계 밖 파일은 절대 수정하지 마라(특히 SecurityConfig, app/api/client.ts, AppShell.tsx, Sidebar.tsx, nav/registry.ts 스켈레톤, 진행상황.md).
- 작업 전 com.pokemo.auth(백엔드)와 app/api/calendarApi.ts 같은 기존 파일을 읽고 패턴을 그대로 따라라.
- 끝나면 백엔드는 backend\mvnw.cmd test, 프론트는 npm run lint && npm run test && npm run build 통과를 확인해라.
```

### D — 프롬프트 0 (기반 리팩터, 가장 먼저)
```
[공통 머리말]
목표: 병렬 개발 충돌 제거를 위한 0단계 기반 작업. 브랜치 feat/foundation.

백엔드:
1) com.pokemo.common 패키지 생성:
   - ApiException(RuntimeException, HttpStatus status, String message + getter)
   - ErrorResponse record(String message, int status, OffsetDateTime timestamp) + of() 팩토리
   - GlobalExceptionHandler(@RestControllerAdvice): ApiException만 처리해 ErrorResponse 반환.
     (검증 예외는 기존 AuthExceptionHandler가 전역 처리 중이므로 중복 핸들러 만들지 마라)
   - CurrentUserProvider(@Component): Principal을 받아 UserAccountRepository.findByEmail로 long userId 반환. 없으면 ApiException(401).
2) com.pokemo.subject 패키지: Subject 엔티티(id, name, color)+SubjectRepository.
   ApplicationRunner 또는 data 시드로 미적분/자료구조/영어회화/운영체제/데이터베이스 5개 초기 삽입(중복 방지).
프론트:
3) app/api/client.ts: base URL(VITE_API_BASE_URL) + pokemo.auth 토큰 자동 헤더 + 에러를 ApiError로 변환하는 fetch 래퍼. lib/authApi.ts 패턴 재사용.
4) pokemoApi.ts를 app/api/{subjectApi,calendarApi,noteApi,quizApi,statsApi,aiApi,noticeApi}.ts로 분리(각 도메인 함수 이동, 당장은 mock 반환 유지 가능).
5) types.ts를 app/types/{common,calendar,note,quiz,stats,ai,notice}.ts로 분리 후 app/types/index.ts에서 re-export(기존 import 안 깨지게). ViewId 유니온은 제거하고 id를 string으로.
6) nav 레지스트리: app/nav/types.ts(NavEntry{id,label,icon,section,element}), app/nav/registry.ts(각 screens/{도메인}/nav.tsx를 합치는 배열).
   AppShell.tsx는 활성 id의 element 렌더, Sidebar.tsx는 registry를 section별로 렌더(하드코딩 NAV_SECTIONS 제거).
7) 기존 DashboardScreen 1개만 새 구조(screens/dashboard/ + nav.tsx + registry 등록)로 마이그레이션해 시범 보이기.
검증: mvnw.cmd test, npm run lint/test/build 통과. 끝나면 변경 요약을 출력(다른 팀원에게 공지할 내용).
```

### A — 프롬프트 (인증/관리자/공지)
```
[공통 머리말]
목표: 인증 보강 + 공지 도메인. 브랜치 feat/auth. 소유 경계: com.pokemo.auth, com.pokemo.notice, app/screens/{notice,auth}/, app/api/noticeApi.ts, app/types/notice.ts. SecurityConfig는 내가 전담.

백엔드:
1) 이메일 인증(F-02): EmailVerification 엔티티/코드 생성·검증 API(POST /api/auth/email/verify-request, /verify). 발송은 우선 콘솔 로그/Mock 인터페이스로 추상화.
2) 로그아웃/refresh revoke(POST /api/auth/logout): AuthToken 무효화.
3) 비밀번호 찾기(F-05): 재설정 토큰 생성·검증·변경 API.
4) 공지 CRUD(F-08): com.pokemo.notice에 Notice 엔티티(title, body, tag enum, pinned, viewCount, author, createdAt), 목록/상세 공개 조회 + 작성/수정/삭제는 ADMIN. 컨트롤러는 com.pokemo.common.ApiException 사용.
5) SecurityConfig: 공지 GET은 permitAll, 공지 작성/수정/삭제와 /api/admin/**는 ADMIN. 새 public 경로(이메일 인증/비번찾기)도 여기서 permitAll 추가. (이 파일은 나만 수정)
프론트:
6) app/screens/notice/: 공지 목록·상세, 관리자용 작성/수정/삭제 화면. app/screens/auth/: 비밀번호 찾기 화면.
7) app/api/noticeApi.ts(client.ts 사용), app/types/notice.ts.
8) app/screens/notice/nav.tsx 만들고 app/nav/registry.ts에 import 1줄만 추가(다른 줄 수정 금지).
검증: mvnw.cmd test(공지 CRUD/권한 테스트 포함), npm run lint/test/build.
```

### B — 프롬프트 (일정/노트)
```
[공통 머리말]
목표: 과목/일정/노트 도메인. 브랜치 feat/calendar-note. 소유 경계: com.pokemo.calendar, com.pokemo.note, com.pokemo.subject(CRUD 확장만), app/screens/{calendar,note}/, app/api/{calendarApi,noteApi,subjectApi}.ts, app/types/{calendar,note}.ts.
전제: com.pokemo.subject.Subject 엔티티와 com.pokemo.common.CurrentUserProvider는 0단계에서 생성됨 — 그대로 주입해 사용.

백엔드:
1) Subject CRUD API(/api/subjects) — 0단계 엔티티 위에 컨트롤러/서비스만 추가.
2) 일정(com.pokemo.calendar): CalendarEvent 엔티티(title, subjectId, startAt, endAt, allDay, type, reminder, recurrence) + CRUD. 반복 일정(RecurrenceRule)과 응답에 dDay 계산 포함(F-09~F-11). 사용자별 데이터는 CurrentUserProvider로 userId 필터.
3) 노트(com.pokemo.note): Note 엔티티(title, subjectId, content, tagIds, updatedAt) + CRUD. 태그(Tag) 추가/삭제, 제목·본문 검색 API, 자동저장 PATCH /api/notes/{id}(content 부분 수정)(F-13,F-16~F-18).
프론트:
4) app/screens/calendar/: 캘린더 + D-Day 표시. app/screens/note/: Markdown 에디터(미리보기), 과목 필터, 태그, 검색, 2초 debounce 자동저장 UI.
5) app/api/{calendarApi,noteApi,subjectApi}.ts(client.ts 사용), app/types/{calendar,note}.ts.
6) 각 screens/{calendar,note}/nav.tsx 만들고 nav/registry.ts에 import 1줄씩만 추가.
검증: mvnw.cmd test(일정/노트 CRUD·검색 테스트), npm run lint/test/build.
```

### C — 프롬프트 (퀴즈/통계)
```
[공통 머리말]
목표: 퀴즈/평가/통계 도메인. 브랜치 feat/quiz-stats. 소유 경계: com.pokemo.quiz, com.pokemo.stats, app/screens/{quiz,wrong,stats}/, app/api/{quizApi,statsApi}.ts, app/types/{quiz,stats}.ts.
전제: Subject 엔티티(subjectId)와 CurrentUserProvider는 0단계 산출물 — 주입해 사용.

백엔드:
1) 퀴즈(com.pokemo.quiz): Quiz/Question 엔티티 + 타입별(MCQ/Short/OX) 구조(F-19~21). 풀이 제출/채점 API, Answer 저장.
2) QuizAttempt(점수·정답률 이력, F-23), 오답노트 WrongAnswerNote 자동 분류·조회(F-22).
3) 통계(com.pokemo.stats): 과목/유형별 정답률(F-24), 과목별 성취도(F-32), 정답률 추이(F-33), 주간 공부시간 집계(F-31) API. 모두 CurrentUserProvider로 사용자 한정.
프론트:
4) app/screens/quiz/: 객관식·단답·OX 풀이. app/screens/wrong/: 오답노트. app/screens/stats/: 정답률/성취도/추이/주간시간 그래프(차트는 가벼운 SVG 또는 단순 라이브러리).
5) app/api/{quizApi,statsApi}.ts(client.ts 사용), app/types/{quiz,stats}.ts.
6) 각 screens/{quiz,wrong,stats}/nav.tsx 만들고 nav/registry.ts에 import 1줄씩만 추가.
검증: mvnw.cmd test(채점/통계 집계 테스트), npm run lint/test/build.
```

### D — 프롬프트 2 (AI + 통합 + 배포, 0단계 이후)
```
[공통 머리말]
목표: AI 도메인 + 대시보드 통합 + Docker compose. 브랜치 feat/ai, feat/deploy. 소유 경계: com.pokemo.ai, app/screens/{ai,dashboard}/, app/api/aiApi.ts, app/types/ai.ts, app/nav/registry.ts 스켈레톤, AppShell/Sidebar/client.ts.

백엔드(com.pokemo.ai):
1) AiClient 인터페이스 + GeminiAiClient 구현(RestClient로 Gemini 2.0 Flash generateContent 호출). 설정 pokemo.ai.api-key=${GEMINI_API_KEY:}, model=gemini-2.0-flash. 키는 환경변수만.
2) AI 요약 POST /api/ai/summary(noteId 기반)(F-27), AI 퀴즈생성 POST /api/quiz/generate(노트→프롬프트→JSON 파싱·검증 후 저장, 파싱 실패 시 저장 금지, 타임아웃 503)(F-25), 추천 GET /api/recommend(취약개념·취약과목·시험임박, AI 오류 시 정답률만 반환)(F-26,F-28~F-30).
3) 노트/퀴즈/통계 데이터는 B·C의 API/서비스를 읽어 조합(직접 그 패키지 수정 금지, 필요 시 read 인터페이스 합의).
프론트:
4) app/screens/ai/: 요약 결과·취약개념·추천 화면. app/screens/dashboard/: 일정/노트/퀴즈/추천 통합 대시보드(B·C·A의 api 함수 호출해 조합).
5) mock→실제 API 전환 마무리: 각 app/api/*.ts가 client.ts로 실제 백엔드 호출하도록 정리.
배포:
6) backend/Dockerfile(멀티스테이지, Java 21), frontend/Dockerfile(빌드 후 nginx 정적 서빙), 루트 docker-compose.yml(backend+frontend+mysql, 환경변수: DB_*, JWT_SECRET, GEMINI_API_KEY). 한 번에 docker compose up으로 뜨게.
검증: mvnw.cmd test, npm run lint/test/build, docker compose up 후 로그인→대시보드→AI 동작 수동 확인. 진행상황.md 갱신.
```

---

## 8. 검증 게이트

- **0단계 완료 기준**: `feat/foundation` 머지 후, 마이그레이션한 화면 1개가 새 nav 레지스트리로 정상 렌더 + `npm run build` 통과 + 백엔드 `mvnw.cmd test` 통과.
- **각 PR 게이트**: 백 `backend\mvnw.cmd test` + 프론트 `npm run lint && npm run test && npm run build` 통과 캡처 첨부.
- **통합 검증(D 최종)**: `docker compose up` → 회원가입/로그인 → 대시보드 → 일정 → 노트 → 퀴즈 → AI 요약/추천이 한 흐름으로 동작(발표 시나리오).
- **충돌 없음 확인**: A/B/C 머지 시 공유 파일 충돌이 `nav/registry.ts`의 서로 다른 import 줄 외에는 없어야 함.
