# Pokemo 구현 진행상황

기준 문서:

- `doc/Pokemo_요구사항_문서_v2.3.docx`
- `doc/Pokemo_설계문서_v3_2.docx`

현재 상태: **1단계 - 프로젝트 골격 구성 완료**

## 진행 규칙

- 구현은 아래 단계 순서대로 진행한다.
- 각 단계 시작 시 상태를 `진행 중`으로 바꾸고, 완료 시 `완료`로 갱신한다.
- 기능 구현 시 관련 요구사항 ID(F-xx, NF-xx)를 커밋/PR/작업 메모에 함께 남긴다.
- 단계 완료 기준은 코드 작성뿐 아니라 빌드, 테스트, 문서 갱신까지 포함한다.

## 전체 구현 단계

| 단계 | 상태 | 목표 | 주요 요구사항 |
| --- | --- | --- | --- |
| 0. 저장소/계획 수립 | 완료 | GitHub 원격 연결, 문서 기반 구현 로드맵 작성 | 프로젝트 관리 |
| 1. 프로젝트 골격 구성 | 완료 | React/Spring Boot/MySQL 기반 3-Tier 구조 초기화 | NF-15, NF-16, NF-17, NF-20 |
| 2. 계정/인증 기반 | 대기 | 회원가입, 로그인, JWT, Refresh Token, Role 구조 구현 | F-01~F-07, NF-07, NF-08 |
| 3. 관리자/공지 기능 | 대기 | ADMIN 권한 공지사항 CRUD와 접근 제어 구현 | F-07, F-08 |
| 4. 과목/일정 관리 | 대기 | 과목, 캘린더 일정, 반복 일정, D-Day 계산 구현 | F-09~F-11 |
| 5. 알림 기반 | 대기 | 이메일 인증/비밀번호 재설정/웹 푸시 알림 기반 구현 | F-02, F-05, F-12 |
| 6. 노트 기능 | 대기 | Markdown 노트, 이미지 첨부, 과목 분류, 태그, 검색, 자동 저장 구현 | F-13~F-18 |
| 7. 퀴즈/평가 기능 | 대기 | 객관식/단답형/OX, 풀이 기록, 오답노트, 정답률 통계 구현 | F-19~F-24 |
| 8. AI 서비스 계층 | 대기 | GPT/Gemini 연동 추상화, AI 퀴즈 생성, 요약/취약 개념 분석 구현 | F-25~F-30, NF-21 |
| 9. 대시보드/시각화 | 대기 | 주간 공부 시간, 과목별 성취도, 정답률 추이, 통합 대시보드 구현 | F-31~F-34 |
| 10. 보안/성능 보강 | 대기 | SQL Injection, XSS, CSRF, 로그, 페이지네이션, 캐시 대응 | NF-01~NF-14, NF-18, NF-19 |
| 11. 인프라/배포 | 대기 | Docker, Nginx, AWS, GitHub Actions, DB 백업 구성 | NF-27~NF-31 |
| 12. 최종 검증 | 대기 | 통합 테스트, 문서 정리, 배포 점검, MVP 완료 판정 | 전체 |

## 1단계 세부 계획: 프로젝트 골격 구성

상태: **완료**

완료 기준:

- Frontend React 프로젝트 생성
- Backend Spring Boot 프로젝트 생성
- MySQL 연동 설정 초안 작성
- 개발/운영 환경변수 분리
- 기본 REST API 헬스체크 작성
- Swagger/OpenAPI 기본 설정
- 최소 빌드 및 테스트 명령 확인

작업 메모:

- 설계 문서 기준 아키텍처는 React SPA, Spring Boot REST API, MySQL, Redis, S3, Nginx, Docker, GitHub Actions로 구성한다.
- 초기 구현은 핵심 MVP 흐름을 빠르게 검증하기 위해 인증, 일정, 노트, 퀴즈, AI, 대시보드 순서로 진행한다.
- Frontend는 Vite React TypeScript로 구성했고 `npm run test`, `npm run build` 통과를 확인했다.
- Backend는 Spring Boot 3.4.1, Java 21 target, Maven 기반으로 구성했다.
- Backend dev/prod profile과 MySQL datasource 환경변수 설정 초안을 작성했다.
- Actuator `/actuator/health`, API `/api/health`, Swagger UI `/swagger-ui/index.html` 구성을 추가했다.
- Backend는 `mvnw.cmd test`, `mvnw.cmd package` 통과를 확인했다.
- Java LSP 진단은 로컬 `jdtls` 미설치로 실행하지 못했고, Maven 컴파일/테스트로 대체 검증했다.

## 2단계 세부 계획: 계정/인증 기반

상태: **대기**

완료 기준:

- User/Member/Admin/Credential/AuthToken 도메인 모델 구현
- 이메일/비밀번호 회원가입 구현
- bcrypt 비밀번호 해싱 적용
- JWT Access Token과 Refresh Token 발급/검증 구현
- USER/ADMIN Role 기반 접근 제어 구현
- 로그인 예외 처리: 비밀번호 불일치 401, 이메일 없음 404

## 3단계 세부 계획: 관리자/공지 기능

상태: **대기**

완료 기준:

- ADMIN 전용 공지사항 작성/수정/삭제 구현
- 일반 사용자의 관리자 API 접근 차단
- 공지 목록/상세 조회 API 구현

## 4단계 세부 계획: 과목/일정 관리

상태: **대기**

완료 기준:

- Subject 모델 구현
- CalendarEvent 모델 구현
- 일정 등록/조회/수정/삭제 API 구현
- RecurrenceRule 기반 반복 일정 초안 구현
- 시험/과제 마감일 기준 D-Day 계산 구현

## 5단계 세부 계획: 알림 기반

상태: **대기**

완료 기준:

- 이메일 인증 코드 발송/검증 흐름 구현
- 비밀번호 재설정 링크 발송 흐름 구현
- PushNotification 모델 및 알림 예약 구조 구현
- 일정 임박 알림 발송 인터페이스 구현

## 6단계 세부 계획: 노트 기능

상태: **대기**

완료 기준:

- Note/Tag/Attachment/NoteVersion 모델 구현
- Markdown 저장 및 렌더링 흐름 구현
- 이미지 업로드 저장소 추상화 구현
- 과목별 필터링, 태그 검색, 제목/내용 검색 구현
- 2초 debounce 자동 저장 API 구현
- JWT 만료 시 자동 갱신 흐름과 충돌하지 않도록 설계

## 7단계 세부 계획: 퀴즈/평가 기능

상태: **대기**

완료 기준:

- Quiz/Question/Answer/QuizAttempt/WrongAnswerNote 모델 구현
- MultipleChoiceQuestion, ShortAnswerQuestion, OxQuestion 타입 구현
- 퀴즈 생성/풀이/채점/기록 API 구현
- 틀린 문제 자동 오답노트 저장
- 과목/유형별 정답률 통계 계산

## 8단계 세부 계획: AI 서비스 계층

상태: **대기**

완료 기준:

- AI Provider 인터페이스로 GPT/Gemini 교체 가능 구조 구현
- 노트 기반 프롬프트 조립
- AI 퀴즈 JSON 파싱 및 검증
- AI 타임아웃 503 처리
- AI 오류 시 기본 데이터만 제공하는 fallback 설계
- 학습 요약, 취약 개념 분석, 취약 과목/시험 임박 추천 API 구현

## 9단계 세부 계획: 대시보드/시각화

상태: **대기**

완료 기준:

- 주간 공부 시간 집계 API 구현
- 과목별 성취도 계산 API 구현
- 퀴즈 정답률 추이 API 구현
- 대시보드 통합 조회 API 구현
- 모바일 브라우저를 고려한 반응형 UI 구현

## 10단계 세부 계획: 보안/성능 보강

상태: **대기**

완료 기준:

- ORM/PreparedStatement 기반 SQL Injection 방어 확인
- 입력/출력 이스케이프 및 XSS 방어 확인
- CSRF 토큰 또는 SameSite 쿠키 정책 적용
- 인증 실패/비정상 접근 보안 로그 기록
- 페이지네이션과 인덱스 적용
- Redis 캐시 적용 대상 선정 및 구현

## 11단계 세부 계획: 인프라/배포

상태: **대기**

완료 기준:

- Frontend/Backend Dockerfile 작성
- docker-compose 개발 환경 구성
- Nginx Reverse Proxy 및 SSL 종단 설정 초안 작성
- GitHub Actions 빌드/테스트 파이프라인 작성
- AWS EC2/RDS/S3 배포 절차 문서화
- DB 자동 백업/복구 시나리오 정리

## 12단계 세부 계획: 최종 검증

상태: **대기**

완료 기준:

- 주요 유스케이스 수동 검증
- Backend 단위/통합 테스트 통과
- Frontend 빌드 통과
- API 문서와 구현 상태 일치 확인
- `진행상황.md` 최종 상태 갱신

## 변경 이력

| 일시 | 내용 |
| --- | --- |
| 2026-05-26 | GitHub 원격 연결 및 문서 기반 구현 로드맵 최초 작성 |
| 2026-05-26 | 1단계 프로젝트 골격 구성 완료: React frontend, Spring Boot backend, MySQL profile config, health check, Swagger, 최소 빌드/테스트 확인 |
